@model Blog.Models.UserCreatedPost

@{
    ViewBag.Title = "Details";
}

<div id="body" class="container">
    <div class="row">
        <div class="col-md-12">

            <div class="row">
                <div class="group1 col-md-8">
                    <h2>@Model.Tittle</h2>

                    @Html.ActionLink("Sua bai viet", "Edit", @Model.PostId)
                    <br />
                    <br />
                    <span class="glyphicon glyphicon-user"></span> @Model.UserName &emsp;
                    <span class="glyphicon glyphicon-time"></span> @Model.CreatedDate.ToString("dd/MM/yyyy")

                </div>
            </div>
            <br />
            <p class="test">@Model.Content</p>

            <hr>

        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>

<h4>Leave a comment </h4>
@Html.Action("Comment")




    @*@Html.HiddenFor(model => model.PostId)
    @Html.HiddenFor(model => model.CommentId)
    <div class="form-group">
        @Html.LabelFor(model => model.UserId, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EditorFor(model => model.UserId)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.contentcomment, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.TextAreaFor(model => model.contentcomment)
            @Html.ValidationMessageFor(model => model.contentcomment)
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>*@


@section scripts {
    @*<script type="text/javascript">
            $('#comment').keypress(function (e) {

                if (e.which == 13) {
                    var dt = new Date();
                    var time = dt.getDate() + '/' + dt.getMonth() + '/' + dt.getFullYear() + ': ' + dt.getHours() + ":" + dt.getMinutes();

                    var a = $('<li><div class="clearfix"><h4>' + $('input#name').val() + '</h4></div> \ <p><em>' + $('textarea#comment').val() + '</em> \ <p class="pull-right">' + time + '</p> </li>');
                    console.log(a);
                    $('ul#comments').append(a);
                    e.preventDefault();
                }
            });
        </script>*@
}








